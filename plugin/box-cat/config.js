const path = require('path')
module.exports = {
  // entry: path.join(__filename , '../index.js'),
  // output: path.join(__filename , '../dist'),
  identifier: {
    data: 'data',
    http: 'http'
  }
}